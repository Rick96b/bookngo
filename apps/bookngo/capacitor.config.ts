import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bookngo.angular',
  appName: 'Bookngo',
  webDir: '../../dist/apps/bookngo/browser',
};

export default config;

